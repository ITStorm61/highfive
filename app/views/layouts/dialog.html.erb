<!DOCTYPE html>
<html>
<head>  
  <title><%= full_title(yield(:title)) %></title>  
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<div class="container">
    <div class="parent_popup">
        <div class="popup">
            <nav class="navbar dialog-bar"> 
                  <div class="fleft">
                  
                      <%= link_to "back", tasks_path, :class => "btn nav-btn" %>
                    </div>

                    <div class="header">
                      Title
                    </div>
                    <div class="fright">
                    <%= yield(:nav_right_button) %>
                    </div>             

            </nav>
            <%= yield %>
        </div>
    </div>
</div>

</body>
</html>
